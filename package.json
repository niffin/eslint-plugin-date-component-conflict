{
  "name": "eslint-plugin-date-component-conflict",
  "version": "0.0.1",
  "description": "make sure no one's using the native Date object when importing the <Date> component",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "niffin",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha tests --recursive"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "mocha": "^6.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC",
  "dependencies": {
    "requireindex": "^1.2.0"
  }
}
